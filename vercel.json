{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm install && cd apps/web && pnpm prisma generate && pnpm build",
  "devCommand": "cd apps/web && pnpm dev",
  "installCommand": "pnpm install --shamefully-hoist",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
